-- Run in pgAdmin while connected to the default "postgres" database (Query Tool).
-- Creates the application database; then connect to it in pgAdmin and run reset_and_create.sql.
-- If you already created "agentFlow" in pgAdmin, skip this — just run reset_and_create.sql there.

CREATE DATABASE "agentFlow";
